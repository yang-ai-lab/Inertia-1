# Core runtime dependencies. For an editable install use `pip install -e .`
# (which reads pyproject.toml). This file is a convenience mirror.
numpy==1.26.4
pyyaml>=6.0
torch>=2.1
pytorch-lightning>=2.2
pandas>=2.0
pyarrow>=12.0
tqdm>=4.66
scikit-learn>=1.3
matplotlib>=3.7
scipy>=1.10

# Optional: experiment logging
# wandb>=0.16
