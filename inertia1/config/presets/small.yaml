# Size preset: "small".
# The transformer-sizing knobs under `preset.patchtst` are shared by both
# shipped methods (patchtst -> model.params, ar_transformer -> method.params).
preset:
  patchtst:
    d_model: 512
    n_layers: 4
    n_heads: 4
    d_ff: 1024
